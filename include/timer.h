#ifndef TIMER_H
#define TIMER_H

#include <stdint.h>

/**
 * @brief Returns the current time in milliseconds since the program started.
 * @return The current time in milliseconds.
 */
uint64_t time_ms(void);

#endif